Weight path:C:\Users\Tahn\SoftDevel\trailer_pose_network\weights\experimental\async_space_time_yaw_hist\async_space_time_yaw_hist_v1.pth
Outputs = [dx_body, dy_body, dyaw]
RMSEs at final checkpoint (Epoch 15 of 15):
	Training RMSE: [0.04049798 0.00567615 0.07181887 0.00322437 0.00271641]
	Validation RMSE: [0.03338868 0.00575856 0.0654008  0.00295568 0.00228412]
PRETRAINED = False

# === DATALOADER PARAMETERS ===
NUM_FRAMES = 2
IMG_SIZE = (224,448)
BATCH_SIZE = 6
VAL_RATIO = 0.2
NUM_WORKERS = 4

# === MODEL PARAMETERS ===
NUM_FRAMES = 2
NUM_IMU_SAMPLES = 5
EMBED_DIM = 384
NUM_HEADS = 8
DEPTH = 8
PATCH_SIZE = 16
IN_CHANNELS = 3
IMU_CHANNELS = 8
DROPOUT = 0.
NUM_OUTPUTS = 5

# === TRAINING PARAMETERS ===
NUM_EPOCHS = 15
LR = 3e-5
LOSS_SCALE = [1e0, 3e2, 1e0]
LOSS_FUNC = [nn.MSELoss(), nn.MSELoss(), nn.MSELoss()]
BETAS = (0.9, 0.999)
WEIGHT_DECAY = 0.05
WARMUP_PERIOD = 2 # The number of epochs to warmup
RUN_VAL = True
OVERFIT_DETECTOR = False
CHECK_ACCURACY = True
CHECK_GRADIENTS = False

optimizer = torch.optim.AdamW(model.parameters(), lr=LR, betas=BETAS, weight_decay=WEIGHT_DECAY)
scheduler = torch.optim.lr_scheduler.CosineAnnealingWarmRestarts(optimizer=optimizer, T_0=len(loader_train)*2, T_mult=2)
warmup_period = len(loader_train) * WARMUP_PERIOD
warmup_scheduler = warmup.LinearWarmup(optimizer=optimizer, warmup_period=warmup_period)

Comments:
-Best working model so far for absolute position and orientation (yaw) prediction
-Big assumption for the test right now is that histories are provided from truth. This needs to be test in the full filter implementation!
-Raw preds seem to look good
-Could potentially extend histories and predictions to N and E positions as well ...
